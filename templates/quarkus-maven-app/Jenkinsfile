pipeline {
    agent any
    stages {
        stage('Build') {
          steps { 
              script {
                  build job: 'test-pipeline', wait:false
              }

          }
        }
    }
}
